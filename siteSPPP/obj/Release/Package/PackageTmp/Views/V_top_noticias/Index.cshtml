@model IEnumerable<siteSPPP.Models.V_top_noticias>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TITULO)
        </th>
        <th>

            @Html.DisplayNameFor(model => model.FOTO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.R)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TITULO)
        </td>
        <td>
            <img src="/V_top_noticias/ObtenerFoto/@Html.DisplayFor(modelItem => item.ID_NOTICIA)" alt="" height=100 width=200 />
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.R)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_NOTICIA }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_NOTICIA }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_NOTICIA })
        </td>
    </tr>
}

</table>
